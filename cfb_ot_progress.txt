Project: CFB OT Pricer
File: cfb_ot_app.py
Location: C:/Users/ja.russo/Desktop/BARNEY/CFB OT Pricer/

Overview
--------
A Streamlit app for pricing and tracking College Football overtime markets.
Given two FBS teams, user-defined strength parameters, and a live score, the
app computes drive outcome probabilities, current OT period win/tie/advance
probabilities, and a full game-length market table covering OT 1 through OT 7+.

The app correctly models the two CFB overtime rule regimes:
  - OT 1–2: Full possession OT (each team gets one possession from the 25-yd line).
    Drive outcomes: TD+PAT (7), TD+2pt (8), TD (6 / missed PAT), FG (3),
    Turnover / Missed FG (0), Defensive TD (6 for the defense).
  - OT 3+: 2-pt conversion shootout (each team runs one play from the 3-yd line).
    Outcome per team: Success (2pts) or Failure (0pts).

State is fully managed via st.session_state so the OT history, scores, and
period counter persist through user interactions.

v0.1 Changelog
--------------
- Built complete Streamlit app from scratch, self-contained, no external data files.
- Implemented all 134 FBS teams in a sorted dropdown for Away/Home selection.
- Sidebar controls:
    * Team selection dropdowns (Away / Home).
    * "Overall Strength" slider (-1.0 to +1.0, step 0.05) — shifts TD vs FG/turnover
      rates for the offensive team. Dynamic label shows favored team or "Even".
    * "Off/Def Tendency" slider (-1.0 to +1.0, step 0.05) — scales the overall
      scoring environment up or down. Dynamic label shows game type.
    * Away / Home score inputs (synced with session_state).
    * Compact OT history scoreboard table (standard OT shows TD-7/FG-3/--- labels;
      shootout shows checkmark/X per team).
    * "Possession order" radio — Away first vs Home first per period.
    * Current situation panel:
        - Standard OT: field position slider (1–99), down selectbox, distance input,
          plus per-team outcome selectboxes for logging.
        - Shootout (OT 3+): simplified Success/Failure radio per team.
    * "Log Result" button — records period outcome, updates scores, advances period.
    * "Reset" button — clears all history and resets to OT 1, score 0-0.
- Probability engine (base_drive_probs):
    * Starts from calibrated base rates: td_pat=35%, td_2pt=5%, td_6=3%,
      fg=20%, turnover=32%, def_td=5%.
    * Applies off_def_tendency multipliers to scoring categories then renormalizes.
    * Applies strength_delta multipliers for the specific offense then renormalizes.
    * Away and home each get their own probs (strength_delta sign flips for home).
- period_outcome_probs: full enumeration of all 36 away×home outcome combinations,
  yielding P(away wins period), P(home wins period), P(advances to next OT).
- shootout_period_probs: closed-form P(away wins), P(home wins), P(tied) for the
  2-pt conversion shootout, with success probability scaled by off_def_tendency.
- game_length_table: chains period-level probabilities to produce cumulative
  P(ends this OT) and P(game still live) for OT 1 through OT 7+.
- Main panel sections:
    1. Drive Outcome Probabilities — Altair donut charts for Away and Home drives;
       expandable breakdown table.
    2. Current OT Period Outcome — Altair donut chart + metric cards for
       away wins / home wins / advances.
    3. Game Length Markets — styled DataFrame table + Altair bar chart showing
       the game-length distribution across OT 1–7+.
- No scipy, no external data files; only stdlib + streamlit + altair + pandas.
- Run with: streamlit run cfb_ot_app.py

Fixed — 2026-08-07  (commit d6b217e)
------------------------------------
Go-for-2 when the second possessor is down 7 (JA's tweak to the rational-second
model). Previously a TD that left the second team down 7 FORCED the PAT to tie.
But going for 2 to win it right there is risky and plausible — and it's a coaching
choice, not a base rate, so it's driven by that team's aggressiveness slider.

- New GO_FOR_2_DOWN_7_MAX = 0.60. p(go for 2) = 0.60 * aggressiveness, so the 0.2
  default = 12% go for the win; max slider = 60%. Set the constant to 0.0 to restore
  the old always-kick behavior (kept as the off switch / regression anchor).
- Split deficit 7 out of the old shared deficit-6/7 branch in rational_second_probs.
  The go-for-it slice resolves at TWO_PT_BASE_RATE into 8 (win) / 6 (loss) — i.e.
  ~45/55, "close to 50/50 as it usually is" per JA. Deficit 6 still ALWAYS kicks,
  since there the PAT simply wins.
- Threaded the SECOND possessor's own rating through every consumer so chart and
  price agree: period_outcome_probs_ordered (new second_aggr param),
  _p_away_wins_from_period_start, game_win_probs (Cases B and C), game_length_table,
  and the three render_main call sites. Possession ALTERNATES by period, so each site
  selects the second team's rating specifically (away slider drives away's decision
  only) — this is the easy thing to get backwards if this code is touched again.
- Both aggressiveness slider help strings updated to mention the new effect.

Verified: down-7 PAT share 100%/88%/70%/40% at aggressiveness 0.0/0.2/0.5/1.0;
deficits 8/6/3/0/-6 and all of OT2+ bit-identical regardless of slider;
GO_FOR_2_DOWN_7_MAX=0.0 reproduces prior output exactly; Away up 7 moves -220 ->
-231 as home aggressiveness goes 0 -> 1; AppTest boots clean and TD -> +1 into the
down-7 state raises no exception.

KNOWN AND INTENDED: at the 0.2 default this is -EV. Trailing team is worth 0.4768
by tying and playing on vs 0.4500 for the 2pt try (breakeven 2pt rate = 47.7%). The
knob buys PLAUSIBILITY, not sharpness — cranking it toward 1.0 prices the trailing
team progressively shorter than a rational model would. Flag for traders.

Fixed — 2026-08-02  (commit f54b110)
------------------------------------
Both aggressiveness sliders default to 0.2 instead of 0.0 — teams play more
aggressively in OT. Settled by playtesting; the trader moves it from there if they
disagree. NOTE: this means the "aggressiveness ignores down & distance" limitation
(TODO #1) is no longer opt-in — it's baked into every default price.

Fixed — 2026-07-31  (commit 6a58b3d)
------------------------------------
Round of Kyle's playtest feedback (ran the 2024 Georgia-Georgia Tech game). Three
distinct pieces, all verified via AppTest + pure-fn tests and reviewed on a local
host before push.

1. Rational trailing team (Kyle's core find). A team that scored first left a large
   chunk of the SECOND team's distribution in "FG" — but a trailing team would never
   kick a FG that guarantees a loss, and the model was pricing the leader absurdly
   short (esp. after a first-team 2pt). Approach B (structural), chosen with JA:
   - New helper rational_second_probs(base, deficit, ot_period). Deficit = first
     team's within-period pts minus what the 2nd team already has. Logic:
       * FG dominated (deficit 6/7/8): strip fg mass, model go-for-it (40% -> TD
         split, 60% -> turnover). Deficit 3 keeps FG (ties -> advance); <=0 keeps it.
       * TD try by deficit (pre-OT2): 8 -> must go for 2 (td_2pt/td_6 at
         TWO_PT_BASE_RATE); 7 -> kick PAT to tie (JA's call); 6 -> kick PAT to win;
         0/3 -> normal split. OT2+ mandatory-2pt already overrides in base.
   - Wired into period_outcome_probs_ordered (re-conditions the 2nd team for EACH
     first-team outcome), so current period + future periods + recursion + game-
     length table are all consistent. Also game_win_probs Case C (keyed on the
     logged first result) and the 2nd team's DISPLAY pie (chart matches price — no
     FG slice when trailing by >3).
   - SECOND_POSSESSOR_EDGE dropped 0.17 -> 0.0. The rational-play model now EARNS the
     second-mover edge structurally (~54.2% OT1 / ~53.2% OT2 even matchup), so the
     old artificial knob was double-counting (pushed it to ~57%). Kept as a tunable.
   - Verified: OT1 first-team TD+PAT -> Home pie FG=0, leader ~-217 (was far longer);
     first-team TD+2pt -> leader ~-604; deficit unit tests pass for 0/3/6/7/8.

2. OT3+ shootout strength calibration (Kyle: GT +162 too long once at dueling 2pts).
   Halved the shootout strength coefficient in away_succ/home_succ (strength_delta
   0.10 -> 0.05). A single 2pt play is near coin-flip, so strength should move the
   shootout ML far less than a full-drive OT. Max-strength favorite ~-150 (was ~-227).

3. Field position / down / distance reset on a new possession. The prior "reset" via
   key-deletion INSIDE _log_result ran AFTER the widgets rendered, so Streamlit
   ignored it — nothing actually reset (this is why the earlier "already works" claim
   didn't hold up live). Fix: all three are now pure state-driven widgets (no
   positional/index/value default). reset_field_widgets() (a button on_click CALLBACK
   — fires before the next rerun instantiates widgets) snaps them to 1st & 10 from the
   25 on TD/FG/TO; the pending-TD try buttons (+2/+1/+0) reset via key-delete + rerun.
   Retired the unused prev_ytg/cur_down/cur_dist auto-advance plumbing. Undo button
   renamed "Undo last scoring play".

Kyle feedback NOT actioned (by JA's direction):
- UI 25-yd marker / full auto-reset niceties — not a priority; may not be feasible.
- 2OT wheel still shows a TD+PAT slice at 0% — not a problem on JA's end, left alone.
- Down/distance AUTO-advance as the slider drags never worked; deprioritized. NOTE:
  the auto-advance plumbing was REMOVED in fix #3 above (widgets are now purely
  manual + reset-on-play), so if we ever want auto-advance back it's a rebuild.

Fixed — 2026-07-21  (commit 72138a5)
------------------------------------
Four items from the 7/20 notes, all verified end-to-end (pure-fn tests + Streamlit
AppTest button simulation) before commit:

1. OT3+ shootout price flow-through (was the "prices don't update" bug).
   game_win_probs previously returned a static closed-form shootout price for
   period>=3, ignoring first_possession_logged. Now shootout_period_probs takes
   optional (first_this, first_result) to condition on the first possessor's known
   2-pt attempt, and game_win_probs threads a new first_shootout_result param so an
   "advance" rolls into the unconditioned p_away_shootout. The moneyline, period pie,
   and game-length advance rate all move when the first team goes. Verified: even OT3
   50/50 -> Success 77.5% / Failure 27.5% for the first team.

2. Mandatory 2-pt try in OT2+ (NCAA rule). base_drive_probs gained an ot_period param;
   for period>=2 it zeroes td_pat (7) and splits TD mass into td_2pt (made, 8) /
   td_6 (failed, 6) at TWO_PT_BASE_RATE. New constant TWO_PT_BASE_RATE=0.45 is the
   single source of truth, also now anchoring away_succ/home_succ (was magic 0.47).
   The base/future dists are built at ot_period=2 (the only future STANDARD period),
   live dists use the actual period. UI: pending-TD row shows +2/+0 only (no +1) in OT2+.

3. Play-undo button. UNDO_KEYS snapshot stack in session_state; push_undo() before
   each play (TD press, FG, TO, shootout Success/Failure), pop_undo() restores and
   wipes transient field widgets (resets to 1st & 10 from 25). One undo reverts a full
   play including the pending-TD try. "↩ Undo last play (N)" button above Full Reset,
   disabled when empty; Full Reset clears the stack.

4. Second-possessor edge. SECOND_POSSESSOR_EDGE=0.17 (tunable; 0.0 disables). New
   _apply_second_edge() tilts the SECOND possessor's dist toward scoring; applied
   inside period_outcome_probs_ordered (Case B + game-length + recursion) and in
   game_win_probs Case C. Calibrated so an even matchup's second team wins that OT
   ~52.4% (OT1) / ~52.2% (OT2). Because possession ALTERNATES OT1<->OT2 the edge
   largely cancels at the game level (~50/50) and shows up in per-period pricing —
   matches JA's note that once aggressiveness ratings are dialed in this may emerge
   naturally, at which point drop the constant to 0.
   NOTE: edge is NOT applied to the OT3+ shootout (symmetric already; has its own
   mid-period conditioning). Revisit if a shootout second-mover edge is wanted.

Item verified as ALREADY working: possession reset to 1st & 10 from the 25 — _log_result
deletes the field-position keys on each possession, so sliders revert on rerun; undo
also wipes them via _TRANSIENT_KEYS.

Also fixed a latent bug while here: _apply_second_edge originally used a frozen default
arg (edge=SECOND_POSSESSOR_EDGE bound at def time) — changed to resolve the constant at
call time so retuning the constant actually takes effect.

Fixed — 2026-07-12
------------------
- Moneyline win-prob bug: game_win_probs was reusing the current period's
  live/collapsed drive distributions for all hypothetical FUTURE periods, so
  a team that had just scored was assumed to score again with certainty every
  future OT. This massively over-favored the first team to score (post-TD home
  win prob showed ~19% when it should be ~29%). Fixed by threading separate
  future_away_probs/future_home_probs (the neutral base dists) into the
  recursion. Committed 056ee12. (Even-matched post-TD home win prob now ~29%.)

- Two-step TD scoring: pressing TD no longer scores immediately. It enters a
  pending state (pending_td) that (a) converges the ball team's drive
  distribution onto the three TD point totals 6/7/8, renormalized, in both the
  drive pie and the moneyline, and (b) swaps the TD/FG/TO button row for a
  +2 / +1 / +0 row. Those finalize as td_2pt (8), td_pat (7), td_6 (6)
  respectively via the existing _log_result path, so scoring stays correct for
  both first- and second-possession cases. Verified win probs: Away-first TD
  then +1 -> home 29%, +2 -> home 2%, +0 -> home 55%. Full Reset clears
  pending_td cleanly. Shootout (OT3+) flow unchanged.

TODO — Next Session
-------------------
1. Aggressiveness × Down & Distance (NOT STARTED)
   Aggressiveness currently applies a fixed adjustment regardless of field
   situation. Ideally its impact should scale with down & distance — e.g. a
   4th-and-1 should drive far more go-for-it (FG suppression) than a
   4th-and-15, and late-down situations should influence the FG-vs-TD/TO
   split more than 1st down. Deferred as too complex for now.

2. Aggressiveness — score-aware 2pt decisioning (PARTIALLY DONE)
   DONE 2026-08-07: the SECOND possessor's down-7 case is now score-aware — they
   go for 2 to win at GO_FOR_2_DOWN_7_MAX * aggressiveness instead of always
   kicking (see the 2026-08-07 entry above). Deficits 8 and 6 were already
   score-aware (forced 2pt / forced PAT respectively).
   STILL OPEN: the FIRST possessor plays blind, so their 8-vs-7-vs-6 split is
   still a flat function of the slider with no score input — which is arguably
   correct (they have no deficit to respond to), so the real open question is
   whether the first team's split should instead key off game state (period
   number, prior OT results) rather than score.

3. OT3+ Moneyline Odds — data upgrade  (PARTIALLY DONE; rest BLOCKED on data)
   DONE 2026-07-21: shootout moneyline now updates mid-period (conditions on the
   first team's logged attempt) and the 2-pt success rate is anchored on the
   shared TWO_PT_BASE_RATE (0.45) instead of a magic 0.47.
   STILL OPEN: swap the flat base rate for actual historical 2-pt conversion
   rates + team-strength adjustments so the game-length advance rates reflect
   real data rather than a league constant.

4. Second-possessor edge — calibration follow-up  (OPEN, low priority)
   SECOND_POSSESSOR_EDGE is a flat 0.17 constant. Once the aggressiveness ratings
   are dialed in, the second-mover advantage should emerge behaviorally — at that
   point reduce/zero the constant so we're not double-counting. Also decide
   whether the shootout (OT3+) wants its own second-mover edge (currently none).

5. Injections and Automations  (BLOCKED — needs live data feed; discuss first)
   Auto-populate field position / down / distance from a live feed (ESPN API
   or similar) so the user doesn't hand-track the drive. Also consider
   auto-advancing the period when both possessions are logged.

Done / Verified
---------------
- Neutral defaults: added generic "AWAY" (orange #E8710A) and "HOME"
  (green #2E7D32) placeholder teams at the top of the dropdown, with matching
  TEAM_COLORS/TEAM_ABBR entries. App now opens on AWAY vs HOME instead of
  Alabama vs Georgia; real teams still selectable below.
- OT3+ shootout UI (was TODO #3): team-colored Success/Failure buttons,
  first-possession badge, tracker-box integration — implemented and live.
- Aggressiveness sliders (UI + model): two side-by-side team-color-coded
  sliders under Off/Def Tendency, 0.0-1.0 (default 0.0 = normal play).
  Preserved across Full Reset. Now WIRED into base_drive_probs via an
  `aggressiveness` param: (1) suppresses FG (~85% of FG mass removed at a=1),
  reallocating it 60/40 to TD/turnover; (2) shifts the within-TD point split
  from 87/8/5 (pat/2pt/6) toward 55/33/12 as a rises. Applied to the ACTIVE
  drive ONLY (the ball team's _live dist) — base/future distributions always
  use aggressiveness=0, so future OTs are unaffected until they become active.
  Verified: a=0 reproduces prior output exactly; FG 0.20->0.03 and TD/TO both
  rise at a=1.
- Sidebar compaction: injected CSS to trim top padding, widget gaps, caption
  and header margins so controls fit with less scrolling. (CSS targets
  Streamlit internal DOM — re-verify if Streamlit version changes.)
